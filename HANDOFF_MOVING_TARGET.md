# 移动靶可视化交接记录

> **最新接续入口（2026-09-04）：** 请先阅读 `HANDOFF_MOVING_TARGET_NEXT.md`。本文件保留完整演进和审计历史，前半部分的旧文件名、旧阈值和阶段性 benchmark 不代表当前实现。

## 当前项目

工作目录：`C:\Users\ASUS\Desktop\rm`

主程序：`sp_vision_moving_target_visualizer.py`

启动命令：

```powershell
Set-Location C:\Users\ASUS\Desktop\rm
python .\sp_vision_moving_target_visualizer.py
```

自测命令：

```powershell
python .\sp_vision_moving_target_visualizer.py --self-test
```

## 最初的 sp_vision_25 复现

这部分是算法复现，不是最早的 Git 作业提交记录。

- 源仓库：`sp_vision_25\`，对应 `TongjiSuperPower/sp_vision_25`。
- 依据仓库中的 `tasks/auto_aim/planner/planner.cpp`、`planner.hpp`、`configs/standard4.yaml` 和 TinyMPC 实现。
- 已编译仓库 TinyMPC 为 `sp_vision_tinympc.dll`，由 `sp_vision_tinympc_bridge.cpp` 提供调用桥接。
- 可视化程序通过 `ctypes` 调用 DLL，不是用 Python 重写一个替代 MPC。
- 复现参数：`DT=0.01 s`、`HORIZON=100`、`Q_yaw=[9e6,0]`、`R_yaw=[1]`、云台角加速度约束 `±50 rad/s²`、TinyMPC 10 次迭代、`shoot_offset=2`（约 `20 ms`）。
- 目标预测遵循源项目 `Target::predict` 的一阶模型：角度按当前角速度外推，未把闪避方未来角加速度送入射手预测器。
- 延迟按源代码使用 `15 ms` 档位；弹丸飞行时间按距离除以 `25 m/s` 计算。
- 源仓库只给出 `plan.fire` 控制信号和云台反馈接口，没有下位机真实机械射频实现；当前 `20 发/秒` 是仿真假设。
- 复现边界：二维可视化，忽略高度、俯仰、重力下坠、炮口偏移、云台柔性、齿隙和真实延迟抖动。

复现相关文件：`sp_vision_25\`、`sp_vision_tinympc_bridge.cpp`、`sp_vision_tinympc.dll`、`sp_vision_moving_target_visualizer.py`。

## 代码结构与运行链路

### `sp_vision_moving_target_visualizer.py`

- 基础数学函数：`rotate`、`norm`、`angle_error`、`wrap_angle`、`clamp`。
- 动力学：
  - `advance_full_acceleration()`：以最大角加速度积分，并在 `±600°/s` 边界反向；
  - `advance_optimized_evasion()`：在上述边界约束外，每 `1.50 s` 主动切换加速度方向。
- 数据结构：
  - `TargetState`：目标中心位置、平移速度、角度、角速度、角加速度；
  - `ArmorPose`：四块装甲的位置、切向量和法向量；
  - `Shot`：一次射击的发射/命中时间、瞄准点、实际发射角和结果；
  - `PlannerOutput`：TinyMPC 输出的 yaw、速度、加速度、开火标志和预测轨迹。
- 几何与预测：
  - `armor_poses()`：由车体尺寸和旋转角生成四块装甲；
  - `predict_constant_velocity()`：复现源项目的一阶目标预测；
  - `nearest_armor()`：按距离选择最近装甲，保持与源代码一致。
- 规划器：`TinyMpcPlanner.plan()` 先加入延迟，再按弹速计算飞行时间，生成前后 `0.5 s` 的参考 yaw/速度轨迹，通过 DLL 调用真实 TinyMPC，最后取 `shoot_offset=2` 的开火误差。
- 射击与命中：
  - `shot_from_plan()`：用发射瞬间实际云台角度生成弹丸；
  - `evaluate_shot()`：检查弹丸是否与装甲线段相交，并计算入射法向速度；
  - 有效命中条件为法向速度 `>12 m/s`。
- 界面主循环：`MovingTargetVisualizer.tick()` 每帧按以下顺序执行：

  `advance_actual → update_planner → advance_gimbal → fire_if_ready → resolve_shots → draw`

- 云台：`advance_gimbal()` 用规划绝对 yaw、yaw velocity、yaw acceleration 做受 `±50 rad/s²` 限制的简化闭环；橙线绘制实际云台方向，紫线绘制规划射击方向。
- 开火：`fire_if_ready()` 只使用源程序 `plan.fire` 许可和 `SHOT_INTERVAL=0.05 s` 射频限制；预测误差显示在状态栏，但不额外拦截射击。
- 统计：`resolve_shots()` 累计有效命中、低法向速度无效和脱靶，界面显示有效命中率。
- 自测：`self_test()` 覆盖预测、TinyMPC 输出、速度边界、有效/低速/脱靶命中分类和射击条件。

### `optimize_evade_strategy.py`

- `Policy` 描述固定或随机加速度换向间隔；
- `BangBangController` 执行候选策略；
- `run()` 使用真实 TinyMPC、当前延迟、弹速和命中判定跑单个距离/时长/随机种子；
- `aggregate()` 汇总多个距离和种子；
- `candidate_policies()` 生成待比较策略；
- `main()` 输出各策略有效命中率并选择最低者。

当前已验证的最优候选是固定 `1.50 s` 反向。

### `validate_suggestions.py`

独立复核建议文件中的入射角、装甲相位窗口、视线角加速度和静止云台对照，不参与主程序运行。

### DLL 桥接

`sp_vision_tinympc_bridge.cpp` 将仓库 TinyMPC 封装为 `sp_solve_yaw` C 接口，Python 使用 `ctypes` 传入参考 yaw/速度、初始状态和输出数组。

## 当前仿真模型

- 2D 平面模型，忽略高度、俯仰和弹道下坠。
- 靶车中心固定在射手正前方：`x=0`，不左右平移。
- 靶车只绕中心旋转。
- 初始角速度：`+300°/s`。
- 目标角速度限制：`±600°/s`。
- 目标角加速度绝对值始终为最大值：`280°/s²`。
- 当前闪避策略：每 `1.50 s` 主动反向一次；若提前触及 `±600°/s`，立即反向，保证不越界。
- 距离滑块：`2–8 m`，改变距离会清空旧弹丸和命中统计。

## 云台控制

代码参考 `TongjiSuperPower/sp_vision_25`：

- TinyMPC 通过 `sp_vision_tinympc.dll` 调用。
- 预测步长：`10 ms`。
- 预测时域：`100` 步。
- `standard4.yaml` 中的云台最大角加速度：`50 rad/s²`。
- `fire_thresh=0.003 rad`。
- 橙线表示实际云台方向，不是规划角度瞬移。
- 实际云台角度、角速度通过规划的绝对 yaw、yaw velocity 和 yaw acceleration 做加速度受限跟踪。
- 实际发射方向使用发射瞬间的实际云台角度。
- 角度显示归一化到 `-180°~+180°`，避免显示多圈累积值。

## 预测与开火

- 目标预测采用当前角速度外推，和源项目 `Target::predict` 的一阶预测一致；闪避方真实运动仍有角加速度，因此可产生预测误差。
- 规划中考虑检测/控制延迟和弹丸飞行时间。
- 开火许可使用 TinyMPC 的 `plan.fire`。
- 额外的实际云台 `3 mrad` 门控已经移除；此前该门控会把射频错误压低。
- 当前仿真假设弹速为 `25 m/s`，不考虑飞行减速。
- 当前射频上限为 **20 发/秒**：

```python
SHOT_INTERVAL = 0.05
```

## 命中判定与统计

- 弹丸与装甲板几何相交后才算接触。
- 有效命中条件：弹丸速度在装甲法向的入射分量 `>12 m/s`。
- 不使用固定 `45°` 入射角规则。
- 结果颜色：
  - 绿色：有效命中；
  - 橙色：接触但法向速度 `≤12 m/s`，无效；
  - 红色：未命中。
- 界面统计：有效命中率、有效命中数、低速无效数、脱靶数、已判定数。
- 命中标记保留 `0.6 s`。

## 已做验证

### 代码检查

```powershell
python -m py_compile .\sp_vision_moving_target_visualizer.py
python .\sp_vision_moving_target_visualizer.py --self-test
```

两项均通过。

### 闪避策略搜索

搜索脚本：`optimize_evade_strategy.py`

它使用真实 TinyMPC、当前弹速、延迟、射频和命中判定，对不同满加速度换向策略进行比较。

在 `2/4/6/8 m`、每个距离 60 秒的测试中，固定 `1.50 s` 反向优于原来的速度边界反向：

- 原边界反向综合有效命中率约 `69.61%`；
- 固定 `1.50 s` 反向约 `47.57%`。

随机换向（例如 `1.35–1.65 s`）没有优于固定 `1.50 s`。

### 建议文件复核

复核脚本：`validate_suggestions.py`

已确认：

- `acos(12/25)=61.314598°` 的代数结果正确；
- 云台连续视线角加速度低于 `50 rad/s²` 的方向成立，但建议中的精确数值不能全部复现；
- 建议文件中的 `|H|=30.699°`、`7.9 m` 等数字没有被当前模型完整复现，不能当作已验证参数；
- “云台静止 + 等待窗口”与当前 TinyMPC `plan.fire` 不是同一个方案，未擅自替换主程序。

## 重要已知限制

- 当前云台执行器是可视化用的简化加速度受限模型，不是完整下位机伺服模型。
- 源仓库没有提供机械射频的真实实现；`20 发/秒` 是本仿真的用户指定假设。
- 源仓库没有提供炮口偏移、云台柔性、齿隙和延迟抖动参数，因此不能据此声称已经完全复现真实硬件。
- `plan.fire` 是源程序的跟踪误差判据，不是完整的“可达集包含于命中窗口”判据。
- 改变射速、弹速、装甲尺寸或云台反馈模型后，应重新运行策略搜索和命中率验证。

## 历史运动概率瞄准模块

主程序中新增 `MotionHistorySample` 和 `ProbabilisticAimModel`，实现了以下基础链路：

- 从 `(时间戳、位置、速度)` 轨迹样本反推固定预测时域内的平均加速度；
- 按当前速度和 `±600°/s` 速度边界裁剪历史加速度分布；
- 采样得到未来位置分布；
- 用 6 m 静止命中率 90% 标定的一维高斯散布计算装甲命中概率；
- 对未来位置分布与散布命中曲线求期望，并返回最优瞄准位置。

该模块目前作为可测试的独立预测器保留，尚未替换 `TinyMpcPlanner` 的默认开火链路。这样可以先进行同条件 A/B 验证，再决定是否接入实际云台规划。

主程序现在支持两个窗口模式：

- `--mode current`：当前版实际云台连续跟踪；
- `--mode probabilistic`：在线记录历史角度/角速度，生成多峰 `P_hit(yaw,t)` 命中概率场，选择当前可达的最高峰并以概率阈值开火；不再使用未来角度圆周均值。

两种模式都使用 `SHOT_INTERVAL=0.05` 和相同高斯散布；可同时启动用于直观对比。

## 概率规划研究线索

外部方法复核表明，当前问题更适合“多模态预测 + 约束规划”，而不是单一均值跟踪：

- Chance-constrained MPC 通常把概率约束作为可行性条件，并在不可行时采用顺序的安全过滤器；可参考 Li 等人的 CC-MPC-CBF（arXiv:2304.01639）。
- GP-MPC 用模型残差分布传播状态不确定性，再将机会约束纳入 MPC；可参考 Hewing、Kabzan、Zeilinger 的 Cautious MPC（arXiv:1705.10702）。
- 多模态预测在规划中应保留 mixture components，先枚举候选峰/模式，再对每个候选用动力学约束评分；不能把左右峰压成圆周均值。

对应到本项目，下一步应优先实现：

1. 按预测时刻生成 `P_hit(yaw,t)`，而不是只生成单时刻场；
2. 用 beam search / 外层候选峰枚举处理非凸多峰场；
3. 在每条候选云台轨迹上累计 `sum(P_hit)`，同时惩罚控制量和速度；
4. 用实测散布和延迟不确定性做机会约束；
5. 以单位时间有效命中数、发射数和校准误差共同选择参数。

补充检索到的可迁移方案：

- Multi-Modal MPC through Batch Trajectory Optimization（arXiv:2109.10392）：并行优化多个候选轨迹，再按任务代价评分；适合左右装甲概率峰外层枚举。
- Branch MPC（arXiv:2109.05128）：为不同未来模式构造分支轨迹，并用风险度量调节保守程度。
- Gaussian-Mixture chance-constrained MPC（arXiv:2401.03799）：对混合高斯不确定性提供 nominal、robust、contingency 三种规划层级；本项目可优先采用 contingency 思路，在主峰失去可达性时切换次峰。

实现修正：概率场的入射有效性现在直接使用 `ArmorPose.normal_x/normal_y` 计算，而不是把装甲中心径向角当作法向角。后者会把坐标系中的前装甲法向方向判反，显著压低概率版命中率。当前按单位时间有效命中数扫描后，默认 `PROBABILITY_FIRE_THRESHOLD=0.15`；在 6 m、5 s 快速测试中约为 5.6 发有效命中/s，高于 0.35 和 0.60 阈值。

## 下一步建议

1. 先检查截图中橙线是否始终在预测方向附近，确认没有再次出现角度漂移。
2. 在 `20 发/秒` 下分别记录 `2/4/6/8 m` 的发射数、有效命中率和平均发射间隔。
3. 若要继续实现建议文件中的方向，应单独增加“预测命中窗口/可达集”模块，不要直接把未经验证的角度窗口常数写入 TinyMPC。
4. 若要更接近真实云台，应从实际下位机反馈中加入最大角速度、炮口偏移和延迟抖动。

## 2026-09-03 规划与性能修正

- 概率版不再把概率峰直接交给零速度 PD。每次先由 TinyMPC 求出完整的速度/加速度受限轨迹，再对整条轨迹施加概率峰相位偏置；这样保留多峰选择能力，也保留原程序的云台动态响应。
- 概率版不再为相位偏置再次调用 TinyMPC DLL，避免每个 10 ms 双重求解造成动画卡顿；每个概率场仍按 50 ms 缓存更新。
- 概率开火条件为 `TinyMPC fire` 与 `P_hit >= PROBABILITY_FIRE_THRESHOLD` 的交集，射频上限仍为 20 发/s。当前阈值 `0.15` 来自单位时间有效命中快速扫描，后续长时 benchmark 应继续验证。
- “固定 1.50 s 反向”是让目标更难命中的闪避策略搜索结果，不是射手最优参数。射手 benchmark 应固定同一目标轨迹，分别统计有效命中数、发射数、单发命中率和有效命中/s。
- 概率峰选择现保留局部峰分支，并加入小连续性代价、切换裕量 `0.035` 和 `0.72` 的相位平滑，避免随机采样导致左右峰来回跳转。
- 概率场使用 1.5° 粗扫 + 局部细化；未来样本数仍为 64，散布和入射法向判据不变。装甲几何改为每个未来样本只计算一次，实测 100 次规划场计算由约 9.3 s 降至约 2.5 s。
- 新增 `benchmark_moving_target.py`，无界面统一执行 current/probabilistic A/B，统计 `valid`、`fired`、有效命中率和 `valid/s`。一次 2/6 m、5 s、同种子测试：current `4.40 valid/s`，probabilistic `4.00 valid/s`；概率版尚未证明优于原版，后续调参必须以长时、多种子结果为准。
- 概率场已从未归一化高斯峰值改为装甲有限宽度区间的高斯积分，与 6 m 静止 90% 散布标定一致。概率版仍直接选择满足可达约束的最高概率峰，不加入额外概率增益门。

## 2026-09-03 完整模型对比版最新状态

用户明确不要“移动标记点”抽象模型；后续工作必须基于完整二维射击模型。完整模型的真实性边界是：射手固定、靶车中心固定、车体自转、四块装甲板、真实云台受角加速度限制、检测/控制延迟、弹丸飞行时间、二维高斯散布、法向速度有效性判定。

新增文件：

- `sp_vision_moving_target_visualizer_full_compare.py`
- `open_full_compare.py`

启动完整对比：

```powershell
Set-Location C:\Users\ASUS\Desktop\rm
python .\open_full_compare.py
```

它打开两个完整可视化窗口：

- `--mode current`：恒速度预测/实际云台连续跟踪基线；
- `--mode probabilistic`：历史加速度概率场选择参考相位。

两个模式共享同一目标动力学、射速 `20 发/s`、延迟、TinyMPC 执行器限制和固定随机种子 `20260903`。概率模式不能清零参考速度/加速度，也不再使用独立零速度 PD；没有额外概率增益开火门，开火只使用 TinyMPC 的 `plan.fire`。

关键真实性修正：计划创建时只保存计划距离和散布样本；队列延迟结束、实际 `launch_at` 时才读取真实云台角度并生成弹丸方向。不能把预测角度冒充实际发射方向。自转只更新 `TargetState.angle` 和四块装甲姿态，不代表目标中心公转。

## 最新可复现实验结果

命令：

```powershell
python .\sp_vision_moving_target_visualizer_full_compare.py --benchmark
```

该 benchmark 与 GUI 使用相同的 `0.01 s` 更新顺序：
`advance_actual -> update_planner -> advance_gimbal -> fire_if_ready -> resolve_shots`。

固定 6 m、30 s 回放结果：

| 方案 | 已判定 | 有效命中 | 有效命中率 | 有效命中/s |
|---|---:|---:|---:|---:|
| current | 386 | 140 | 36.27% | 4.667 |
| probabilistic | 386 | 127 | 32.90% | 4.233 |

结论：在这版修正后的完整模型中，概率版目前低于基线；此前约 `4.39/s` 等数字来自不同实现/开火条件，不能与本结果混用。后续任何“概率版更好”的结论都必须由同一目标轨迹、同一散布随机序列、同一射速和同一实际发射方向规则下的长时多种子 benchmark 证明。

代码自测：

```powershell
python .\sp_vision_moving_target_visualizer_full_compare.py --self-test
```

已通过。当前桌面保留两个新完整模型窗口，旧的重复窗口已关闭。

## 下一对话优先事项

1. 先不要再改动画抽象或目标几何；先用 `--benchmark` 做至少 60 s、多个固定种子的 A/B 统计。
2. 以“单位时间有效命中数”为主指标，“有效命中率”为辅指标，同时记录发射数和已判定数。
3. 若优化概率版，只允许改变概率预测/参考轨迹选择；不能改变实际发射方向读取、散布、射速、法向速度判定或执行器约束。
4. 每次参数变更都要同时跑 `--self-test` 和 benchmark，再打开可视化检查车体是否原地自转、橙线是否为实际云台方向。

## 所有测试过的版本总表

以下结果按“模型是否为完整四装甲射击模型”区分。不同模型的命中率不能直接横向比较。

### 1. 最初完整复现版

文件：`sp_vision_moving_target_visualizer.py`

- 目标中心固定，车体绕中心自转，四块装甲板完整建模。
- 使用源项目 TinyMPC、15 ms 延迟、25 m/s 弹速、20 发/s 射频上限。
- 有真实云台受角加速度限制的跟踪、弹道飞行时间、散布和法向速度判定。
- 这是“最初完美复现原程序”的基准版本。
- 后续加入概率模块后，该文件仍作为原始基线参考，不应被覆盖。

### 2. 下载版概率开火修正版

文件：`C:\Users\ASUS\Downloads\sp_vision_moving_target_visualizer_probability_fire_fixed.py`

- 外观和射击链路仍是完整四装甲模型。
- 曾测得：当前连续跟踪约 `3.89 valid/s`，概率版约 `4.39 valid/s`。
- 真实性审查发现三个问题：概率版用了独立零速度 PD；计划角度被直接当成实际发射方向；两种模式开火条件不一致。
- 因此这组结果被判定为不公平，不能作为最终结论。

### 3. 纯二维标记点 v2 实验

文件：`C:\Users\ASUS\Downloads\moving_target_tracking_experiment_v2.py`

- 仅跟踪二维角度/标记点覆盖，不含四块装甲、弹丸、散布和有效命中判定。
- 包含延迟、理想/受限执行器、历史预测和事件 CSV。
- 受限执行器测试中恒速度基线通常优于历史概率；历史模型曾出现约 `59°` 预测误差。
- 不能把覆盖率或角度误差当作完整射击命中率。
- 用户已明确不要将此抽象模型作为最终可视化。

### 4. 纯二维标记点 v3 安全模型

文件：`C:\Users\ASUS\Desktop\rm\moving_target_tracking_experiment_v3.py`

- 在标记点模型上加入概率预测修正上限 `±10°`。
- 15 ms 延迟、20 个种子、5 s 正式评估：恒速度覆盖率 `1.25%`（约 `0.25/s`），历史概率 `5.90%`（约 `1.18/s`）。
- 该结果只说明抽象跟踪覆盖率，不代表完整射击命中率。
- 可视化曾把自转误画成目标公转，窗口已关闭，后续不要继续使用其可视化。

### 5. 旧主程序概率模式

文件：`sp_vision_moving_target_visualizer.py --mode probabilistic`

- 完整四装甲模型上加入历史平均加速度样本、约束分布和 `P_hit` 概率场。
- 早期版本曾使用概率门控（如 `P_hit >= 0.15`）、较低概率场更新频率和较少样本来缓解卡顿。
- 用户后来明确要求回退概率增益门；这些旧参数和旧数值不再作为当前标准。

### 6. 当前完整模型修正版 A/B

文件：`sp_vision_moving_target_visualizer_full_compare.py`

- 当前推荐的完整模型对比版本。
- `current` 与 `probabilistic` 共用目标轨迹、TinyMPC 执行器、延迟、散布、射速和命中判定。
- 概率版只改变参考瞄准相位，不清零速度/加速度，不使用独立 PD，不增加概率开火门。
- 计划创建时只保存计划信息；实际 `launch_at` 时读取真实云台角度，再生成弹丸方向。
- 6 m、固定种子、30 s benchmark：

  - current：`140/386`，有效命中率 `36.27%`，`4.667 valid/s`；
  - probabilistic：`127/386`，有效命中率 `32.90%`，`4.233 valid/s`。

- 当前结论：在公平、真实性修正后的完整模型中，基线优于概率版；概率版尚未证明有收益。

### 7. 当前完整模型启动器

文件：`open_full_compare.py`

- 同时启动版本 6 的 `current` 和 `probabilistic` 两个完整可视化窗口。
- 两个进程固定随机种子 `20260903`，便于复现散布序列。
- 启动命令：`python .\open_full_compare.py`。

### 8. 当前 benchmark 解释

- 最终指标固定为“有效命中率”和“单位时间有效命中数”，其中单位时间有效命中数优先。
- “已判定”只包括已经到达命中时刻的弹丸；未到达的队列弹丸不进入分母。
- 每次比较必须固定距离、目标闪避轨迹、射速、弹速、随机种子和执行器限制。
- 早期 `3.89/s`、`4.39/s`、`5.6/s` 等结果来自不同版本或不同开火逻辑，不能与当前版本的 `4.667/s`、`4.233/s` 直接混用。

### 9. 2026-09-03 长时多种子复核

使用当前完整模型、6 m、60 s、相同目标轨迹参数和 20 发/s 射频上限，对三个固定散布种子分别运行 `benchmark(60.0)`。每个模式均发射 753 发，低速无效数均为 0：

| 种子 | 方案 | 有效命中 | 有效命中率 | 有效命中/s |
|---:|---|---:|---:|---:|
| 20260903 | current | 285 | 37.85% | 4.750 |
| 20260903 | probabilistic | 264 | 35.06% | 4.400 |
| 20260904 | current | 274 | 36.39% | 4.567 |
| 20260904 | probabilistic | 263 | 34.93% | 4.383 |
| 20260905 | current | 278 | 36.92% | 4.633 |
| 20260905 | probabilistic | 271 | 35.99% | 4.517 |

三种子平均：`current = 4.650 valid/s`、`37.05%`；`probabilistic = 4.433 valid/s`、`35.33%`。因此在当前完整模型和公平开火链路下，概率版仍未优于恒速度基线；后续优化必须继续以长时多种子 benchmark 为依据。

### 10. 固定状态概率对齐诊断

新增命令：

```powershell
python .\\sp_vision_moving_target_visualizer_full_compare.py --diagnostic
```

该诊断固定 `angle=0°`、`omega=300°/s`、`alpha=-280°/s²`、预测时域 `255 ms`，检查加速度分支到未来绝对角度的映射。当前输出：

- 恒速度：`76.500000°`；
- 负加速度主分支：`67.396500°`（相对修正 `-9.103500°`，采样质量约 `96.19%`）；
- 正加速度次分支：`85.603500°`（相对修正 `+9.103500°`，采样质量约 `3.81%`）；
- 加权均值：`68.090187°`，距负分支约 `0.694°`。

这验证了当前概率模型的基础映射没有把主峰错误地变成恒速度点或宽模糊分布。若完整规划仍出现约 26° 相位误差，应继续检查概率图时间戳、规划索引、绝对/相对角转换、旧峰跟踪和重复传播，而不是先调高概率阈值。

### 11. 概率规划 phase 对齐修复

已执行 phase 空间修复：概率缓存现在保存相对当前 TinyMPC nominal yaw 的 `phase`，缓存命中时按当周期 `current_plan.target_yaw + phase` 重建绝对 yaw；分支连续性和 `0.72` 平滑也均在 phase 空间进行，不再锁定或平滑旧的世界坐标 yaw。benchmark 初始化和 reset 路径同步更新。

验证：`--self-test` 与 `--diagnostic` 均通过。固定种子 20260903、6 m、30 s benchmark：current `140/386`、`4.667 valid/s`；修复后 probabilistic `125/386`、`4.167 valid/s`。因此修复消除了明确的绝对 yaw 缓存错误，但尚未使概率规划优于基线；下一步应实现真正以概率场为目标的 TinyMPC/MPC 重规划，而不是对基线轨迹做统一相位平移。

### 12. 概率参考轨迹 TinyMPC 重规划

概率模式不再对已经求解的 `PlannerOutput` 做数组平移。`update_planner()` 先生成同周期基线以构造概率场，再将选中的概率峰通过 `aim_yaw_override` 送回 `TinyMpcPlanner.plan()`，重新构造参考 yaw/速度并再次调用真实 TinyMPC DLL。

同时修复了 override 中心索引：`reference_yaw[i]` 使用 `absolute_yaws[i+1]`，因此撞击时刻的 padded-array 索引应为 `PLANNER_HALF_HORIZON + 1`，旧代码少了一帧。概率中心 phase 现在按恒加速度形式从当前时刻的 0 二次增长至撞击时刻的选定 phase；撞击后保持该 phase。这样不会把只在约 255 ms 内有效的历史平均加速度错误外推到 MPC 额外的 0.5 s 尾部。曾验证过不加时域限制的外推会把约 9° 修正放大至约 80°，并使 30 s 结果降至 `2.933 valid/s`，该错误方案已撤销。

验证：`py_compile`、`--self-test`、`--diagnostic` 均通过。6 m、60 s、三个固定种子结果：

| 种子 | 方案 | 有效命中 | 已判定 | 有效命中率 | 有效命中/s |
|---:|---|---:|---:|---:|---:|
| 20260903 | current | 285 | 753 | 37.85% | 4.750 |
| 20260903 | probabilistic-replan | 266 | 752 | 35.37% | 4.433 |
| 20260904 | current | 274 | 753 | 36.39% | 4.567 |
| 20260904 | probabilistic-replan | 253 | 752 | 33.64% | 4.217 |
| 20260905 | current | 278 | 753 | 36.92% | 4.633 |
| 20260905 | probabilistic-replan | 250 | 752 | 33.24% | 4.167 |

三种子平均：current `4.650 valid/s`、`37.05%`；probabilistic-replan `4.272 valid/s`、`34.08%`。结论是动态一致的二次 TinyMPC 求解已经实现，但“单个撞击时刻概率峰展开成一条参考轨迹”仍未优于基线。严格意义上的概率场 MPC 仍需为多个未来时刻生成 `P_hit(yaw,t)`，枚举分支轨迹，并按整条 TinyMPC 输出的累计概率收益选择，而不是只优化一个中心峰。

### 13. 概率模式独立开火门控

按用户要求，概率模式已取消 TinyMPC `3 mrad` 的 `plan.fire` 开火限制，改为 `current_aim_probability >= 0.50`；基线仍仅使用原始 `plan.fire`。两种模式继续共用 `SHOT_INTERVAL=0.05 s`，实际发射方向、延迟、散布和命中判定不变。概率值现在在最终选择和 phase 平滑之后，重新对实际选中 yaw 计算，避免用平滑前粗网格峰值通过阈值。界面会分别显示 `P>=0.50` 或基线 `3mrad` 门控状态。

回归测试明确覆盖三种情况：概率低于 0.50 时抑制；概率等于 0.50 且 `plan.fire=False` 时允许；基线 `plan.fire=False` 时抑制。`py_compile`、`--self-test`、`--diagnostic` 均通过。

6 m、60 s、三个固定种子结果：

| 种子 | 方案 | 有效命中 | 已判定 | 有效命中率 | 有效命中/s |
|---:|---|---:|---:|---:|---:|
| 20260903 | current-3mrad | 285 | 753 | 37.85% | 4.750 |
| 20260903 | probabilistic-P0.50 | 261 | 900 | 29.00% | 4.350 |
| 20260904 | current-3mrad | 274 | 753 | 36.39% | 4.567 |
| 20260904 | probabilistic-P0.50 | 271 | 900 | 30.11% | 4.517 |
| 20260905 | current-3mrad | 278 | 753 | 36.92% | 4.633 |
| 20260905 | probabilistic-P0.50 | 248 | 900 | 27.56% | 4.133 |

三种子平均：current `4.650 valid/s`、`37.05%`；probabilistic-P0.50 `4.333 valid/s`、`28.89%`。新门控相对上一版概率重规划的 `4.272 valid/s` 略有提升，但仍未超过基线，并明显降低单发有效命中率。

### 14. 禁止用参考峰概率直接开火

这是一个反复出现的语义错误，后续修改必须保留以下区别：

- `current_aim_probability` / `Pref` 是概率规划器所选参考峰的概率，只能用于规划诊断；
- 弹丸实际发射方向来自受 `GIMBAL_MAX_ACCEL=50 rad/s²` 限制的 `self.gimbal_yaw`；
- 概率开火门控必须重新计算实际云台方向的 `current_fire_probability` / `Pgun`，不能用 `Pref` 代替；
- 正确语义是“当前真实炮口方向的命中概率至少为阈值”，不是“规划器希望瞄准的位置概率很高”。

代码已在字段定义和 `fire_if_ready()` 旁加入禁止误用的注释，并加入回归测试：即使 `Pref=1.0` 且 TinyMPC `plan.fire=False`，`Pgun<0.50` 仍必须抑制；`Pgun=0.50` 时概率模式允许开火；基线仍由 `plan.fire` 决定。

修正后 6 m、种子 20260903、30 s 初测：current `140/386`、`36.27%`、`4.667 valid/s`；probabilistic-Pgun0.50 `124/312`、`39.74%`、`4.133 valid/s`。相较错误的 `Pref` 门控，实际方向门控减少了低质量射击并提高单发命中率，但当前阈值下单位时间有效命中仍低于基线。

6 m、60 s、种子 20260903/20260904/20260905 长测中，概率模式每次均判定 611 发，有效命中分别为 246/258/246，对应 `4.100/4.300/4.100 valid/s` 和 `40.26%/42.23%/40.26%`。三种子平均为 `4.167 valid/s`、`40.92%`；同期基线平均 `4.650 valid/s`、`37.05%`。结论：`Pgun>=0.50` 的语义正确且稳定提高单发质量，但阈值偏保守，降低了主指标单位时间有效命中数。

### 15. 缓存 phase 与 Pref 的时间一致性

概率场完整粗扫仍按 50 ms 缓存，但命中缓存时，绝对参考 yaw 会由“当前 `current_plan.target_yaw + cached_phase`”每 10 ms 重建。旧实现同时返回缓存生成时的 probability，导致新绝对 yaw 与旧 probability 不对应；`SHOT_INTERVAL=0.05 s` 与缓存周期相同也不能保证开火时刻相位锁定。

现已保留 phase 缓存，但在每个 10 ms tick 对重建后的参考 yaw 做一次 `count=64` 单点概率评估，更新 `current_aim_probability/Pref`。`Pgun` 本来就在 `fire_if_ready()` 中按真实 `self.gimbal_yaw` 每拍重算，因此本修复不改变当前开火结果。新增回归测试验证缓存命中后 probability 确实使用新绝对 yaw 重新求值；`py_compile`、`--self-test` 通过，6 m、30 s benchmark 与修复前一致。

### 16. 概率峰装甲距离与碰撞语义复核

Opus 提出的“概率峰可能对应另一块装甲，但 `planned_range` 仍取 nominal nearest armor”问题成立。新增 `armor_nearest_yaw()`：在撞击时刻 nominal 几何中先筛选满足法向速度有效条件的装甲，再按中心方位与 `aim_yaw_override` 的角误差选择装甲。override 生效时，`planned_range` 和 `armor_index` 均改用该板，不再无条件沿用距离最近的板。回归测试使用 45° 车体姿态选择另一块有效朝向装甲，验证输出距离和索引对应 override 方位。

“把 `evaluate_shot()` 从撞击时刻点距改为无限射线—板求交”的建议不成立，未执行。当前仿真在 `impact_at` 同时取得弹丸位置 `shot.aim` 和该时刻装甲姿态，点到有限板段的距离是带时间约束的离散碰撞判定。静态射线求交会忽略飞行时间，把弹丸在更早或更晚时刻经过该半径也判为命中。已加入反例测试：弹丸终点位于同一射线上、但为装甲半径两倍时必须脱靶。

`py_compile`、`--self-test` 通过。6 m、种子 20260903、30 s：current 仍为 `140/386`、`4.667 valid/s`；probabilistic 从距离修正前 `124/312` 变为 `123/312`、`4.100 valid/s`。该问题真实存在，但不是当前概率模式性能差距的主要来源。注意：当前按 nominal impact geometry 匹配概率峰对应板，若以后概率模型返回显式 mixture branch/armor ID，应直接传递该 ID，而不是再从 yaw 反推。

### 17. 飞行时间、概率 horizon 与事件时刻闭环修复

经独立验证，Opus 指出的 `flight_time`/`planned_range` 分属不同装甲和固定 255 ms 概率 horizon 两项问题成立，但其“一次重算”补丁不完整。现实现 `armor_solution_for_yaw()`：分别对四个装甲 ID 求解距离—飞行时间固定点，筛选法向速度有效的板，再按概率峰方位选择。返回的 `selected`、`impact_state`、`flight_time` 和 `planned_range` 属于同一装甲与同一时刻，并由 self-test 强制验证 `flight_time * BULLET_SPEED == planned_range`。

概率场每次 50 ms 完整刷新时，将 `ProbabilisticAimModel.horizon` 设置为当前计划的 `delay + flight_time`。若概率峰改选装甲导致飞行时间变化，会最多迭代三次“概率场刷新 → 装甲/飞行时间重规划”，直到 horizon 差小于 `1e-5 s`。phase 始终相对同周期 baseline yaw，避免迭代时再次混淆参考系。

15 ms 发射延迟问题也成立，但不能把它当成 20 ms 或直接读取下一拍末尾 yaw。仿真现在保存每个 10 ms 步长的云台起始状态和实际指令加速度，在 `launch_at` 位于步内时用解析积分取得精确 15 ms 发射角；目标也在 `impact_at` 用同一动力学做步内重建，不再拿下一拍末尾姿态代替撞击时刻。开火门从当前真实云台状态外推 15 ms 到发射方向。10 s 审计中，门控预测与精确子步发射角平均误差 `0.039°`、95 分位 `0.157°`、最大 `0.209°`，该近似在当前控制器下成立。

Opus 所称的 nearest-armor 换板会制造巨大伪参考速度，在当前模型中未复现：60 s、600000 个参考速度样本最大为 `184.24°/s`，超过 `600°/s` 的样本为 0，因此未修改源项目轨迹选择逻辑。

三个 P2 问题已处理：`motion_history` 限制为实际使用的最近 80 点；散布标准差提升为 `SCATTER_SIGMA` 常量，不再每发构造模型；`add_trajectory()` 改为单调双指针，不再为每个样本创建历史切片。

`py_compile`、`--self-test`、`--diagnostic` 均通过。完整时间修复后 6 m、种子 20260903、30 s：current `138/386`、`35.75%`、`4.600 valid/s`；probabilistic-Pgun0.50 `80/209`、`38.28%`、`2.667 valid/s`。修复后 0.50 阈值明显过严；旧阈值结果基于错位的 horizon/发射时刻，不能沿用，下一步应重新扫描阈值并以 valid/s 为主指标。

### 18. 禁止把不可行概率质量裁剪到边界

原 `constrained_accelerations()` 对每个历史平均加速度执行 `clamp(value, lower, upper)`。当当前角速度接近上限时，大量不可行的正加速度会全部变成同一个 `upper`，人为制造边界原子概率、低估分布宽度并抬高峰值置信度。

现改为条件筛选：只保留位于当前允许区间内的历史加速度，保留样本按其原有重复频数重新采样，等价于对剩余经验概率质量重新归一化。不再把拒绝样本投影到边界，也不加入人为 fallback 先验；若没有任何历史样本支持当前状态，`future_position_samples()` 返回空分布，`hit_probability_field()` 对所有 yaw 返回 0，概率门自然抑制开火。

新增边界回归测试：历史包含一个 `-ALPHA_LIMIT` 和两个 `+ALPHA_LIMIT` 样本，当前速度接近正上限时必须只留下负加速度，不得在 `upper` 产生两个重复样本。`py_compile`、`--self-test`、`--diagnostic` 通过。

6 m、种子 20260903、30 s：基线保持 `138/386`、`35.75%`、`4.600 valid/s`；概率模式由边界筛选前 `80/209`、`38.28%`、`2.667 valid/s` 变为 `63/122`、`51.64%`、`2.100 valid/s`。这说明旧边界投影确实放出了较多低质量射击；筛选提高了单发质量，但 `Pgun>=0.50` 在新概率语义下更加保守，必须重新扫描阈值。

### 19. Opus 级联故障审计逐项裁决

- P0“50 ms 内概率峰随 300°/s 车体自转移动 15°”混淆了车体姿态角和射手视线角。目标中心固定在 6 m，60 s 实测 nominal yaw 在 50 ms 内平均变化 `1.55°`、99 分位 `3.37°`、最大 `3.49°`，不是 15°；缓存 phase 每拍加到最新 baseline yaw，概率也已单点重算。horizon 迭代中的 phase 参考系已固定为同周期 baseline，不存在所述“新 armor yaw + 旧参考系 phase”。将完整场刷新从 50 ms 改为 10 ms 的 30 s A/B 反而从 `62/122`、`2.067 valid/s` 降到 `53/135`（另有 1 低速无效）、`1.767 valid/s`，且运行显著变慢，因此保留 50 ms。
- P1 建议使用历史样本自身速度做约束是错误条件。历史样本在采集时已是实际物理轨迹；预测要计算的是给定“当前 omega”后哪些经验加速度仍可行，必须按当前速度做条件筛选。当前代码已删除 clamp/边界投影，不能恢复该旧实现。
- P2 对平滑公式的解释有代数错误：`new = old + 0.72*(target-old)` 中新值权重为 72%，旧值权重为 28%，不是相反。完全取消平滑（系数 1.0）的 30 s A/B 为 `60/132`、`45.45%`、`2.000 valid/s`，低于默认 0.72 的 `62/122`、`50.82%`、`2.067 valid/s`，因此保留默认值。
- P3 建议重新叠加 `plan.fire_error` 不成立。该误差是 TinyMPC 内部参考轨迹与优化解的误差，不是简化真实云台执行器的物理指向误差。当前 `Pgun` 已直接评估由真实 yaw/velocity/acceleration 外推至 15 ms 发射时刻的方向；10 s 审计与精确子步发射角最大误差仅 `0.209°`。不恢复概率模式 3 mrad 门。
- P4 中“主概率场无需 bootstrap”成立，已改为对所有条件筛选后历史样本做确定性经验平均；重复值保留经验频数，结果自然归一化。`count`/`seed` 仅为调用兼容保留，回归测试确认不同 count/seed 输出完全相同。6 m、30 s 为 `62/122`、`50.82%`、`2.067 valid/s`，与 64 bootstrap 的 `63/122` 接近，说明抽样噪声不是性能主因。
- P5 不成立：粗网格是以 gimbal 为中心的 `±180°` 全圆扫描，共 241 个等间距点；中心变化不会漏掉目标区域或降低边缘分辨率，可达性仍相对真实 gimbal 检查。
- P6 不执行：`phase_offset * progress**2` 是恒加速度相对恒速度预测的位移修正，线性 phase 会在当前时刻凭空引入非零速度偏移。`FIRE_LOOKAHEAD=20 ms` 对应中心索引之后的 shoot offset，也不是建议中描述的“撞击前 20 ms”。若以后改变 phase profile，必须先定义新的目标动力学模型，不能为追求 benchmark 直接改指数。

### 20. 历史窗口长度与长期混合实验

“80 点仅覆盖 0.8 s、短于 1.5 s 主动反向间隔”的问题经统计成立。60 s、每 50 ms 建场的分布支持情况：80 点窗口空分布 `15.0%`、单侧 `20.92%`、双侧 `64.08%`；160 点空分布 `1.0%`、单侧 `1.08%`、双侧 `97.92%`；320 点的分支覆盖与 160 点基本相同。

6 m、种子 20260903、30 s A/B：

| 历史方案 | 有效/判定 | 有效命中率 | valid/s |
|---|---:|---:|---:|
| 80 点 | 62/122 | 50.82% | 2.067 |
| 160 点 | 93/203 | 45.81% | 3.100 |
| 320 点 | 94/204 | 46.08% | 3.133 |
| 80/320 各 50% 混合 | 86/197（另 1 低速无效） | 43.65% | 2.867 |
| 80/320 按 25%/75% 混合 | 80/201 | 39.80% | 2.667 |

320 点只比 160 点多 1 发有效命中，但完整场计算时间约翻倍；两种短期/长期混合均低于简单 160 点。无限完整历史还会导致成本随运行时间增长并稀释当前阶段信息，因此未采用。默认改为固定 `MOTION_HISTORY_WINDOW=160`，既覆盖一次 1.5 s 切换又保持有界成本；实验性权重结构已撤销，没有留在主实现中。

### 21. 有符号可达域、细化复核与 Pref 闭环

Opus 第 9 项成立：旧公式 `abs(omega)*t + 0.5*a*t²` 把速度当成左右两侧都有利。新增 `reachable_yaw_delta_bounds()`，在当前执行器没有角速度上限、控制加速度可在 `±50 rad/s²` 内选取的模型下，使用双积分器精确终点区间 `[omega*t - 0.5*a*t², omega*t + 0.5*a*t²]`。候选判断使用以真实 gimbal yaw 为原点的有符号展开角，不再构造对称外包络。

第 10 项部分成立：fine grid 现在重新应用同一有符号可达区间；phase 平滑后和缓存 phase 每拍重建后，都通过 `project_yaw_to_reachable()` 再次投影。当前无速度上限的标量双积分终点集合本身是凸区间，所以“两个可达点的插值必然不可达”不成立，但最终复核仍可防止环绕和细化边界误差。新增测试验证正向高速时整个 100 ms 可达区间仍在正方向，反向候选会投影到有符号下界。

第 11 项未改：撞击前 `phase_offset*progress²` 可由终点 phase 反推恒定有效加速度后积分得到，并非无动力学含义的视觉平滑；它确实只是单一有效加速度轨迹而非完整概率场路径，这属于模型能力边界，不是可直接改成线性的 bug。

第 12 项成立于诊断概率：每轮概率 TinyMPC 重规划完成后，现在用最终 `current_plan.target_yaw` 和最终 `current_plan.predicted_state` 重新计算 `current_aim_probability/Pref`。开火使用的 `Pgun` 原本已经基于最终计划独立计算。

`py_compile`、`--self-test` 通过。6 m、种子 20260903、30 s 与修复前一致：current `138/386`、`35.75%`、`4.600 valid/s`；probabilistic `93/203`、`45.81%`、`3.100 valid/s`。说明漏洞已修，但当前测试轨迹未触发会改变决策的反向误选。
